/** import type {} from '../.sst/platform/config'

// (3) Store the logs. transition to longer-tern storage as data set grows to reduce cost.
export const logBucket = new sst.aws.Bucket('LogBucket')

// ()
export const queryBucket = new sst.aws.Bucket('QueryBucket')

export const outputs = {
    logBucket,
    queryBucket,
}
*/
